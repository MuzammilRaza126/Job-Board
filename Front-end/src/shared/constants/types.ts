export type JobCardProps = {
  job: {
    id: string
    title: string
    company: string
    location: string
    description: string
    requirements: string
    salary: string
    createdAt: string
    updatedAt: string
  }
}
